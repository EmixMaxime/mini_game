#ifndef GRID_H
#define GRID_H

/**
 * 
 * Initialisation de la grille, toutes les cases à 0.
 * C'est la grille en mémoire qui reflète l'état de la grille visuelle
 * @param  grid
 * 
 */

void initializeGrid (int grid[COLUMNS_NUMBER][LINES_NUMBER]);

#endif // GRID_H
